# SC-005: チケット一覧画面

> **ドキュメント種別**: 最新仕様（継続的にメンテナンス）

## 1. 画面基本情報

| 項目 | 内容 |
|------|------|
| 画面ID | SC-005 |
| 画面名 | チケット一覧画面 |
| パス | `/todos/table` または `/todos/table?projectId={id}` |
| Vueコンポーネント | `src/frontend/src/views/TodoTableView.vue` |
| 子コンポーネント | TodoSearchForm, TodoTableFilter, TodoTable |
| 状態管理 | Pinia (`useTodoStore`, `useUserStore`, `useProjectStore`) |
| 技術スタック | Vue.js 3 (Composition API) |
| 目的 | チケットをテーブル形式で一覧表示・フィルタ・ソート |
| 追加案件 | 20251226_チケット一覧画面 |
| イベント詳細 | [events.md](./events.md) |

---

## 2. レイアウト

```
┌─────────────────────────────────────────────────────────────────────┐
│                     チケット一覧                   X / Y 件          │  ← ヘッダー
├─────────────────────────────────────────────────────────────────────┤
│  [検索キーワード入力                      ] [🔍 フィルタ]             │  ← ツールバー
├─────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │ 完了状態: ○すべて ○未完了 ○完了済み                           │ │
│  │ 担当者: [▼選択]  案件: [▼選択]                                │ │  ← フィルタパネル
│  │ 開始日: [    ] 〜 [    ]  終了日: [    ] 〜 [    ]            │ │    （展開時のみ表示）
│  │                                             [リセット]         │ │
│  └───────────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────────────┤
│ ID↑ │ タイトル    │ 担当者    │ 案件      │ 開始日 │ 終了日 │ 状態  │  ← テーブルヘッダー
├─────────────────────────────────────────────────────────────────────┤
│  1  │ タスクA     │ 山田太郎  │ プロジェクトA │ 12/20 │ 12/25 │ [✓][🗑] │
│  2  │ タスクB     │ 鈴木花子  │ -         │ 12/21 │ 12/28 │ [□][🗑] │  ← テーブル行
│  3  │ タスクC     │ -        │ プロジェクトB │ -     │ 12/30 │ [□][🗑] │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. 構成要素

| 要素ID | 要素名 | 種別 | 説明 | 機能 |
|--------|--------|------|------|------|
| E-001 | ヘッダー | テキスト | 画面タイトル「チケット一覧」表示 | - |
| E-002 | 件数表示 | テキスト | フィルタ後件数 / 総件数 表示 | 自動更新 |
| E-003 | フィルタバッジ | バッジ | フィルタ・検索適用中の表示 | 条件付き表示 |
| E-004 | 検索フォーム | input[text] | キーワード検索入力欄 | タイトル・説明検索 |
| E-005 | フィルタボタン | button | フィルタパネル開閉 | パネル表示切替 |
| E-006 | フィルタパネル | パネル | 各種フィルタ条件設定 | 展開/折りたたみ |
| E-007 | 完了状態ラジオ | radio | すべて/未完了/完了済み | フィルタ |
| E-008 | 担当者セレクト | select | 担当者でフィルタ | フィルタ |
| E-009 | 案件セレクト | select | 案件でフィルタ | フィルタ |
| E-010 | 開始日From/To | input[date] | 開始日の期間フィルタ | フィルタ |
| E-011 | 終了日From/To | input[date] | 終了日の期間フィルタ | フィルタ |
| E-012 | リセットボタン | button | フィルタ条件を初期化 | 全条件クリア |
| E-013 | テーブルヘッダー | thead | カラムヘッダー（ソート可能） | ソート切替 |
| E-014 | テーブル行 | tr | チケットデータ行 | 完了切替・削除 |
| E-015 | 完了切替ボタン | button | 完了状態をトグル | PATCH API |
| E-016 | 削除ボタン | button | チケットを削除 | DELETE API |

---

## 4. 遷移

| 遷移元 | トリガー | 遷移先 |
|--------|---------|--------|
| SC-001: ホーム画面 | 「チケット一覧」カード | 本画面（全チケット） |
| SC-002: チケット管理画面 | 「一覧表示」リンク | 本画面 |
| SC-003: プロジェクト画面 | 「一覧表示」ボタン | 本画面（projectId付き） |

---

## 5. UI状態

| 状態 | 条件 | 表示 |
|------|------|------|
| 未完了チケット | completed = false | チェックボックス空 |
| 完了チケット | completed = true | チェックボックス済み |
| ローディング中 | API通信中 | 「読み込み中...」テキスト |
| エラー発生時 | API失敗 | 赤背景のエラーメッセージ |
| データなし | 件数0 | 「チケットがありません」 |
| フィルタ中データなし | フィルタ結果0件 | 「条件に一致するチケットがありません」 |
| フィルタ適用中 | 何らかのフィルタ適用 | フィルタバッジ「フィルタ中」表示 |
| フィルタボタンアクティブ | パネル展開 or フィルタ適用中 | ボタンハイライト |

---

## 6. フィルタ仕様

### 6.1 検索フォーム

| 項目 | 要素ID | 入力タイプ | 対象 |
|------|--------|-----------|------|
| キーワード検索 | E-004 | text | タイトル、説明（部分一致、大文字小文字区別なし） |

### 6.2 フィルタパネル

| 項目 | 要素ID | 入力タイプ | 初期値 | 動作 |
|------|--------|-----------|--------|------|
| 完了状態 | E-007 | radio | すべて | all/pending/completed |
| 担当者 | E-008 | select | 未選択 | null または assigneeId |
| 案件 | E-009 | select | 未選択 | null または projectId |
| 開始日From | E-010 | date | 空 | 開始日 >= 指定日 |
| 開始日To | E-010 | date | 空 | 開始日 <= 指定日 |
| 終了日From | E-011 | date | 空 | 終了日 >= 指定日 |
| 終了日To | E-011 | date | 空 | 終了日 <= 指定日 |

### 6.3 フィルタ適用順序

1. キーワード検索
2. 完了状態フィルタ
3. 担当者フィルタ
4. 案件フィルタ
5. 開始日期間フィルタ
6. 終了日期間フィルタ
7. ソート

---

## 7. ソート仕様

### 7.1 ソート対象カラム

| カラム | ソートキー | デフォルト順序 |
|--------|-----------|---------------|
| ID | id | 昇順（デフォルト） |
| タイトル | title | 昇順 |
| 担当者 | assigneeName | 昇順 |
| 案件 | projectName | 昇順 |
| 開始日 | startDate | 昇順 |
| 終了日 | dueDate | 昇順 |

### 7.2 ソート動作

- 同じカラムをクリック: 昇順⇔降順を切替
- 別カラムをクリック: そのカラムの昇順に変更
- null値は末尾に配置

---

## 8. テーブル仕様

### 8.1 表示カラム

| カラム | 表示内容 | 幅目安 |
|--------|---------|--------|
| ID | チケットID | 60px |
| タイトル | タイトル（テキスト） | auto |
| 担当者 | 担当者名（未設定時は「-」） | 120px |
| 案件 | 案件名（未設定時は「-」） | 150px |
| 開始日 | 開始日（MM/DD形式、未設定時は「-」） | 80px |
| 終了日 | 終了日（MM/DD形式、未設定時は「-」） | 80px |
| 操作 | 完了ボタン、削除ボタン | 100px |

### 8.2 行のアクション

| ボタン | 動作 | 確認ダイアログ |
|--------|------|--------------|
| 完了切替 | 完了状態をトグル | なし |
| 削除 | チケットを削除 | あり（confirmダイアログ） |

---

## 9. 使用API

| 操作 | API | メソッド | 関連イベント |
|------|-----|---------|-------------|
| チケット一覧取得 | /api/todos | GET | EV-005-01 |
| 案件別チケット取得 | /api/todos?projectId={id} | GET | EV-005-01 |
| ユーザー一覧取得 | /api/users | GET | EV-005-01 |
| 案件一覧取得 | /api/projects | GET | EV-005-01 |
| 完了切替 | /api/todos/{id}/toggle | PATCH | EV-005-07 |
| 削除 | /api/todos/{id} | DELETE | EV-005-08 |

---

## 10. イベント一覧

詳細は [events.md](./events.md) を参照。

| イベントID | イベント名 | トリガー | 処理概要 |
|-----------|-----------|---------|---------|
| EV-005-01 | 初期読込 | ページロード | チケット・ユーザー・案件一覧を取得 |
| EV-005-02 | キーワード検索 | 検索フォーム入力 | タイトル・説明で絞り込み |
| EV-005-03 | フィルタパネル開閉 | フィルタボタン | パネル表示切替 |
| EV-005-04 | フィルタ条件変更 | フィルタ項目変更 | 条件適用してリスト更新 |
| EV-005-05 | フィルタリセット | リセットボタン | 全条件を初期化 |
| EV-005-06 | ソート変更 | カラムヘッダークリック | ソート順変更 |
| EV-005-07 | 完了状態切替 | 完了ボタン | 完了状態をトグル |
| EV-005-08 | チケット削除 | 削除ボタン | チケットを削除 |

---

## 更新履歴

| 日付 | 変更内容 | 関連案件 |
|------|----------|----------|
| 2025-12-26 | 初版作成 | 20251226_チケット一覧画面 |
