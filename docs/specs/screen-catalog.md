# 画面一覧

> **ドキュメント種別**: 最新仕様（継続的にメンテナンス）

## 1. 概要

| 項目 | 内容 |
|------|------|
| フロントエンド技術 | HTML5 / CSS / Vanilla JavaScript |
| API通信 | Fetch API |
| 最終更新日 | 2025-12-22 |

---

## 2. 画面一覧

| ID | 画面名 | パス | 概要 | 追加案件 |
|----|--------|------|------|----------|
| SC-001 | ホーム画面 | `/` | アプリへの入口、ToDoリストへのリンク | 初期構築 |
| SC-002 | ToDoリスト画面 | `/todos.html` | ToDo管理のメイン画面 | 初期構築 |

---

## 3. 画面詳細

### 3.1 SC-001: ホーム画面

| 項目 | 内容 |
|------|------|
| パス | `/` |
| ファイル | Spring Boot テンプレート（HelloController） |
| 目的 | アプリケーションへの入口を提供 |

#### 構成要素

| 要素 | 説明 |
|------|------|
| タイトル | アプリケーション名 |
| リンク | ToDoリスト画面への遷移 |

---

### 3.2 SC-002: ToDoリスト画面

| 項目 | 内容 |
|------|------|
| パス | `/todos.html` |
| ファイル | `src/main/resources/static/todos.html` |
| 目的 | ToDo管理のメイン機能を提供 |

#### レイアウト

```
┌─────────────────────────────────────────────────┐
│                   📝 ToDoリスト                  │  ← ヘッダー
├─────────────────────────────────────────────────┤
│         総数: X   完了: X   未完了: X            │  ← 統計エリア
├─────────────────────────────────────────────────┤
│  新しいToDoを追加                                │
│  [タイトル入力                              ]   │  ← 追加フォーム
│  [説明入力（任意）                          ]   │
│  [追加]                                        │
├─────────────────────────────────────────────────┤
│       [すべて] [未完了] [完了済み]               │  ← フィルタボタン
├─────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────┐ │
│  │ タイトル                                   │ │
│  │ 説明                                      │ │  ← ToDoカード
│  │ 作成日: YYYY/MM/DD                        │ │
│  │ [完了にする] [削除]                        │ │
│  └───────────────────────────────────────────┘ │
├─────────────────────────────────────────────────┤
│               ← ホームに戻る                    │  ← フッター
└─────────────────────────────────────────────────┘
```

#### 構成要素

| 要素 | 説明 | 機能 |
|------|------|------|
| ヘッダー | 画面タイトル表示 | - |
| 統計エリア | 総数/完了数/未完了数を表示 | 自動更新 |
| 追加フォーム | タイトル・説明の入力欄と追加ボタン | ToDo新規作成 |
| フィルタボタン | すべて/未完了/完了済み | 表示切替 |
| ToDoカード | ToDo情報の表示 | 完了切替・削除 |
| フッター | ホームへのリンク | 画面遷移 |

#### UI状態

| 状態 | 表示 |
|------|------|
| 未完了ToDo | 通常表示 |
| 完了ToDo | 背景グレー、取消線、透明度70% |
| ローディング中 | 「読み込み中...」テキスト |
| エラー発生時 | 赤背景のエラーメッセージ（5秒後に自動消去） |

---

## 4. 画面遷移

```
┌─────────────┐          ┌─────────────────┐
│  ホーム画面  │ ──────→ │  ToDoリスト画面  │
│   (/)       │          │  (/todos.html)  │
└─────────────┘          └─────────────────┘
                                  │
                                  │ ← ホームに戻る
                                  ▼
                         ┌─────────────┐
                         │  ホーム画面  │
                         └─────────────┘
```

---

## 5. 使用API

| 画面 | 操作 | API | メソッド |
|------|------|-----|---------|
| ToDoリスト | 一覧取得 | /api/todos | GET |
| ToDoリスト | フィルタ取得 | /api/todos?completed={bool} | GET |
| ToDoリスト | 統計取得 | /api/todos/stats | GET |
| ToDoリスト | 新規作成 | /api/todos | POST |
| ToDoリスト | 完了切替 | /api/todos/{id}/toggle | PATCH |
| ToDoリスト | 削除 | /api/todos/{id} | DELETE |

---

## 6. フォーム仕様

### 6.1 ToDo追加フォーム

| 項目 | 入力タイプ | 必須 | placeholder |
|------|-----------|------|-------------|
| タイトル | text | 必須 | 「タイトルを入力してください」 |
| 説明 | text | 任意 | 「説明を入力してください（オプション）」 |

| 操作 | トリガー |
|------|---------|
| 追加 | 追加ボタンクリック or Enterキー |
| 完了後 | フォームクリア、リスト更新、統計更新 |

---

## 7. ボタン仕様

### 7.1 フィルタボタン

| ボタン | 動作 | 初期状態 |
|--------|------|---------|
| すべて | 全件表示 | アクティブ |
| 未完了 | 未完了のみ | - |
| 完了済み | 完了のみ | - |

### 7.2 ToDoカードボタン

| ボタン | 動作 | 確認 |
|--------|------|------|
| 完了にする / 未完了にする | 完了状態をトグル | なし |
| 削除 | ToDoを削除 | 確認ダイアログあり |

---

## 更新履歴

| 日付 | 変更内容 | 関連案件 |
|------|----------|----------|
| 2025-12-22 | 初版作成（2画面） | 初期構築 |
