# バックエンド基本設計書

## 案件情報

| 項目 | 内容 |
|------|------|
| 案件名 | チケット詳細編集機能 |
| 案件ID | 202501_チケット詳細編集機能 |
| 作成日 | 2026-01-08 |
| 関連要件整理書 | [requirements.md](./requirements.md) |

---

## 1. 概要

### 1.1 本設計書の目的

バックエンドが担う機能・データ・APIの基本設計を定義する。

### 1.2 バックエンドの役割

| 責務 | 本案件での変更 |
|------|---------------|
| データ管理 | **変更なし** |
| ビジネスロジック | **変更なし** |
| API提供 | **変更なし** |
| バリデーション | **変更なし** |

### 1.3 変更なしの理由

本案件はフロントエンドのみの改修であり、以下の理由によりバックエンドの変更は不要である:

1. **既存APIで対応可能**: チケット更新API（`PUT /api/todos/{id}`）が既に実装済み
2. **データモデル変更なし**: 既存のTodoエンティティで必要な属性をすべてカバー
3. **バリデーション済み**: 既存APIでタイトル必須チェック・担当者存在チェックが実装済み

---

## 2. 機能要件

### 2.1 機能一覧

| ID | 機能名 | 概要 | 対応US | 変更 |
|----|--------|------|--------|------|
| F-001 | チケット更新 | 既存のチケット情報を更新 | US-001〜005 | **変更なし**（既存API利用） |

---

## 3. データ要件

### 3.1 データ構造

| データ名 | 概要 | 永続化 | 変更 |
|---------|------|--------|------|
| Todo | チケット情報 | 要 | **変更なし** |

### 3.2 エンティティ（変更なし）

#### Todo

| 属性 | 型 | 必須 | 説明 |
|------|-----|------|------|
| id | Long | 自動 | 一意識別子 |
| title | String | 必須 | タイトル |
| description | String | 任意 | 説明 |
| completed | boolean | 必須 | 完了フラグ |
| startDate | LocalDate | 任意 | 開始日 |
| dueDate | LocalDate | 任意 | 期限日 |
| projectId | Long | 任意 | 案件ID |
| assigneeId | Long | 任意 | 担当者ID |
| createdAt | LocalDateTime | 自動 | 作成日時 |

---

## 4. API要件

### 4.1 エンドポイント一覧

| メソッド | パス | 機能 | 変更 |
|---------|------|------|------|
| PUT | /api/todos/{id} | チケット更新 | **変更なし**（既存API利用） |

### 4.2 利用するAPI詳細

#### PUT /api/todos/{id}（既存）

**リクエストボディ**:
```json
{
  "title": "更新後タイトル",
  "description": "更新後説明",
  "completed": false,
  "startDate": "2026-01-01",
  "dueDate": "2026-01-31",
  "assigneeId": 1
}
```

**レスポンス（成功時）**:
```json
{
  "id": 1,
  "title": "更新後タイトル",
  "description": "更新後説明",
  "completed": false,
  "startDate": "2026-01-01",
  "dueDate": "2026-01-31",
  "projectId": null,
  "assigneeId": 1,
  "assigneeName": "山田太郎",
  "createdAt": "2026-01-08T10:00:00"
}
```

### 4.3 エラーレスポンス

| ステータス | 条件 |
|-----------|------|
| 400 Bad Request | 担当者IDが存在しないユーザー |
| 404 Not Found | 指定IDのチケットが存在しない |

---

## 5. 非機能要件

### 5.1 性能要件

| 項目 | 要件 | 変更 |
|------|------|------|
| API応答時間 | 1秒以内 | **変更なし** |

### 5.2 セキュリティ要件

| 項目 | 対応 | 変更 |
|------|------|------|
| 認証 | なし | **変更なし** |
| 入力バリデーション | 必須チェック、担当者存在チェック | **変更なし** |

---

## 6. 制約条件

| 項目 | 制約 | 変更 |
|------|------|------|
| データ保存 | H2 Database（ファイルモード） | **変更なし** |
| ORM | MyBatis | **変更なし** |

---

## 7. 関連ドキュメント

| ドキュメント | 説明 |
|-------------|------|
| [API-TODO-006](../../specs/api/API-TODO-006.md) | Todo更新API詳細仕様 |

---

## 改版履歴

| 版数 | 日付 | 変更内容 | 変更者 |
|------|------|----------|--------|
| 1.0 | 2026-01-08 | 初版作成（バックエンド変更なしを明記） | Claude |
