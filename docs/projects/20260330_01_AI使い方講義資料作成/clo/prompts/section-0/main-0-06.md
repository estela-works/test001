# main-0-06 生成プロンプト

# スライド背景画像生成 共通仕様

## 利用目的
- 3キャラクター会話劇の解説動画で使用する**背景画像**を生成する
- テキスト（見出し・ラベル・字幕）は Remotion のオーバーレイが描画するため、画像内にテキストを焼き込まない

## デザイン方針
- 1枚の画像には1つの概念・事象のみを描く（1画像1概念ルール）
- 写真風・イラスト風・地図風など、コンテンツに最適なスタイルを選択する
- テキストやラベルを画像内で出来るだけ書かない。固有名詞・国際的略語も最小量とする。
- 人物の肖像は必ずイラスト・似顔絵・アイコンスタイルで描くこと（写真風・フォトリアリスティックな描写は厳禁）
- 実在の人物の顔写真を使用しないこと。シルエット、アバター、シンプルなイラストで代替すること

## プロンプト構造のルール
- 「## 生成指示」セクション: AI への指示。画像内にテキストとして描画しないこと
- 「## コンセプトと文脈」セクション: 演出意図と文脈。画像内にテキストとして描画しないこと。画像の雰囲気・トーンの参考にすること
- 「## 画像と字幕の役割分離」セクション: 画像で示す要素と字幕で示す要素の分離ルール。厳守すること
- 「## 参考データ」セクション: 内容設計データ。テキストをそのまま描画せず、視覚的に表現すること
- 「## 画像の説明」セクション: この指示に従って画像を生成すること


## 生成指示（この内容を画像内にテキストとして描画しないこと）
スタイル: Professional lecture slide, summary table + flow diagram + key statement, clean Japanese typography, flat design
配色:
- Background: #FFFFFF, Heading: #1E352A, Table/boxes: #EDF5F0 bg + #B0BFB6 border, Steps: #28AA3C bold, Arrows: #28AA3C, Separator: #28AA3C 4px, Abstract principle: #6B7E74, Key statement: #1E352A bold large

## コンセプトと文脈（この内容を画像内にテキストとして描画しないこと）
第0章の知的クライマックス。2例で見えた共通パターンを4ステップの因果に抽象化し、最後に「ネイティブとは何か」を一文で定義する。ここで観客が持ち帰るのは、ネイティブとは新技術の採用ではなく前提変化への適応だという理解。
- 講義全体の目的: エージェントAIを活用したシステム開発への転換を提案する。全59枚
- 章の目的: 「○○ネイティブ」の共通構造を発見させ、講義全体への期待を作る
- 前スライド: 0-05 二つのネイティブで見える共通構造。2例をまとめた直後
- 次スライド: 0-07 講義の地図＋ブリッジ。「ではAIネイティブでは何が脱構築されるのか？」と問いを残す
- このスライドの役割: 2例の観察を抽象原理に変え、第1章以降の読み方を与える

## 内容説明（この内容を画像内にテキストとして描画しないこと。スライドの背景知識として理解に使うこと）
インターネットでもクラウドでも、起きていることは同じである。技術革新で高コストだった制約が崩れ、コスト構造が変わり、最適化対象が移動し、古い戦略が脱構築され、新しい戦略が再構築される。

観客に覚えてほしいのは、ネイティブという言葉の本質がツールの習熟ではないことだ。ネイティブとは、新技術導入で目立つことではなく、新しいボトルネックに合わせて実務の判断軸を切り替えることである。

## 画像と字幕の役割分離（厳守）
これは講義用スライド画像。テキストは全て画像内にレンダリングすること。

画像内に描画するテキスト:
- タイトル「○○ネイティブに共通する変化構造」
- メインメッセージ「ネイティブとは新技術導入でなく前提変化に合わせて動くことだ」
- 2枚の要約カード
- 4ステップの構造図
- 最終コールアウト

## 参考データ（テキストをそのまま描画せず、視覚的に表現すること）
```
+================================================================+
|  ○○ネイティブに共通する変化構造                                  |
|  ネイティブとは新技術導入でなく前提変化に合わせて動くことだ      |
|                                                                  |
|  [Internet Native] 配信制約 → 探索制約                           |
|  [Cloud Native]    変更回数最小化 → 変更単位最小化               |
|                                                                  |
|  STEP1   技術革新で制約が変わる                                   |
|    →     今まで高かったコストが一気に下がる                       |
|  STEP2   コスト構造が激変する                                     |
|    →     新しいボトルネックが別の場所に立つ                       |
|  STEP3   最適化対象が移動する                                     |
|    →     旧来の合理性では勝てなくなる                             |
|  STEP4   脱構築と再構築が起きる                                   |
|    →     新しいボトルネックに最適化した実務が生まれる             |
|                                                                  |
|  ○○ネイティブとは、新しいボトルネックに対して                     |
|  最適化する世界観で行動すること                                   |
+================================================================+
```

## 画像の説明
A professional lecture slide that abstracts two historical examples into a shared structure. White background. Top: title "○○ネイティブに共通する変化構造" and message "ネイティブとは新技術導入でなく前提変化に合わせて動くことだ". Upper section: two compact summary cards, one for Internet Native and one for Cloud Native. Main section: a horizontal four-step flow showing constraint change, cost-structure shift, optimization-target shift, and destruction/reconstruction. Bottom: a strong callout stating "○○ネイティブとは、新しいボトルネックに対して最適化する世界観で行動すること". The slide should feel like the chapter's distilled framework. Aspect ratio 16:9.

画像サイズ: 1024x576px
